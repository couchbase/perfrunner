[test_case]
test = perfrunner.tests.n1ql.N1QLWithXdcrInitBackupTest

[showfast]
title = 3 -> 3 (2 source nozzles, 4 target nozzles), Initial index, Backup, 30 bucket x 100K x 1KB, 1000 KV ops/sec
component = multi_bucket
sub_category = BQXB

[stats]
server_processes = goxdcr
client_processes = cbbackupmgr

[cluster]
mem_quota = 45000
index_mem_quota = 100000
initial_nodes = 5 3
num_buckets = 30

[xdcr_cluster]
sourceNozzlePerNode = 2
targetNozzlePerNode = 4
optimisticReplicationThreshold = 0

[xdcr]
wan_delay = 40

[compaction]
db_percentage = 100
view_percentage = 100

[bucket]
replica_number=1
replica_index=0

[secondary]
indexer.settings.storage_mode = plasma
indexer.numPartitions = 2

[load]
items = 100000
size = 1024
workers = 10
key_fmtr = hex

[index]
statements =
    CREATE INDEX idx11 ON `bucket-1`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx12 ON `bucket-1`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx13 ON `bucket-1`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx14 ON `bucket-1`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx15 ON `bucket-1`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx16 ON `bucket-1`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx17 ON `bucket-1`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx18 ON `bucket-1`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx19 ON `bucket-1`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx10 ON `bucket-1`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx21 ON `bucket-2`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx22 ON `bucket-2`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx23 ON `bucket-2`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx24 ON `bucket-2`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx25 ON `bucket-2`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx26 ON `bucket-2`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx27 ON `bucket-2`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx28 ON `bucket-2`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx29 ON `bucket-2`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx20 ON `bucket-2`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx31 ON `bucket-3`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx32 ON `bucket-3`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx33 ON `bucket-3`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx34 ON `bucket-3`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx35 ON `bucket-3`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx36 ON `bucket-3`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx37 ON `bucket-3`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx38 ON `bucket-3`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx39 ON `bucket-3`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx30 ON `bucket-3`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx41 ON `bucket-4`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx42 ON `bucket-4`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx43 ON `bucket-4`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx44 ON `bucket-4`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx45 ON `bucket-4`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx46 ON `bucket-4`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx47 ON `bucket-4`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx48 ON `bucket-4`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx49 ON `bucket-4`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx40 ON `bucket-4`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx51 ON `bucket-5`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx52 ON `bucket-5`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx53 ON `bucket-5`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx54 ON `bucket-5`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx55 ON `bucket-5`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx56 ON `bucket-5`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx57 ON `bucket-5`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx58 ON `bucket-5`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx59 ON `bucket-5`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx50 ON `bucket-5`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx61 ON `bucket-6`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx62 ON `bucket-6`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx63 ON `bucket-6`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx64 ON `bucket-6`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx65 ON `bucket-6`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx66 ON `bucket-6`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx67 ON `bucket-6`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx68 ON `bucket-6`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx69 ON `bucket-6`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx60 ON `bucket-6`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx71 ON `bucket-7`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx72 ON `bucket-7`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx73 ON `bucket-7`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx74 ON `bucket-7`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx75 ON `bucket-7`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx76 ON `bucket-7`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx77 ON `bucket-7`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx78 ON `bucket-7`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx79 ON `bucket-7`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx70 ON `bucket-7`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx81 ON `bucket-8`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx82 ON `bucket-8`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx83 ON `bucket-8`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx84 ON `bucket-8`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx85 ON `bucket-8`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx86 ON `bucket-8`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx87 ON `bucket-8`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx88 ON `bucket-8`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx89 ON `bucket-8`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx80 ON `bucket-8`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx91 ON `bucket-9`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx92 ON `bucket-9`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx93 ON `bucket-9`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx94 ON `bucket-9`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx95 ON `bucket-9`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx96 ON `bucket-9`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx97 ON `bucket-9`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx98 ON `bucket-9`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx99 ON `bucket-9`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx90 ON `bucket-9`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx101 ON `bucket-10`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx102 ON `bucket-10`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx103 ON `bucket-10`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx104 ON `bucket-10`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx105 ON `bucket-10`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx106 ON `bucket-10`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx107 ON `bucket-10`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx108 ON `bucket-10`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx109 ON `bucket-10`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx100 ON `bucket-10`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx111 ON `bucket-11`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx112 ON `bucket-11`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx113 ON `bucket-11`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx114 ON `bucket-11`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx115 ON `bucket-11`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx116 ON `bucket-11`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx117 ON `bucket-11`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx118 ON `bucket-11`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx119 ON `bucket-11`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx110 ON `bucket-11`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx121 ON `bucket-12`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx122 ON `bucket-12`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx123 ON `bucket-12`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx124 ON `bucket-12`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx125 ON `bucket-12`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx126 ON `bucket-12`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx127 ON `bucket-12`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx128 ON `bucket-12`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx129 ON `bucket-12`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx120 ON `bucket-12`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx131 ON `bucket-13`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx132 ON `bucket-13`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx133 ON `bucket-13`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx134 ON `bucket-13`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx135 ON `bucket-13`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx136 ON `bucket-13`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx137 ON `bucket-13`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx138 ON `bucket-13`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx139 ON `bucket-13`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx130 ON `bucket-13`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx141 ON `bucket-14`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx142 ON `bucket-14`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx143 ON `bucket-14`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx144 ON `bucket-14`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx145 ON `bucket-14`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx146 ON `bucket-14`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx147 ON `bucket-14`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx148 ON `bucket-14`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx149 ON `bucket-14`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx140 ON `bucket-14`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx151 ON `bucket-15`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx152 ON `bucket-15`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx153 ON `bucket-15`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx154 ON `bucket-15`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx155 ON `bucket-15`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx156 ON `bucket-15`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx157 ON `bucket-15`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx158 ON `bucket-15`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx159 ON `bucket-15`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx150 ON `bucket-15`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx161 ON `bucket-16`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx162 ON `bucket-16`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx163 ON `bucket-16`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx164 ON `bucket-16`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx165 ON `bucket-16`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx166 ON `bucket-16`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx167 ON `bucket-16`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx168 ON `bucket-16`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx169 ON `bucket-16`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx160 ON `bucket-16`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx171 ON `bucket-17`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx172 ON `bucket-17`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx173 ON `bucket-17`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx174 ON `bucket-17`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx175 ON `bucket-17`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx176 ON `bucket-17`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx177 ON `bucket-17`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx178 ON `bucket-17`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx179 ON `bucket-17`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx170 ON `bucket-17`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx181 ON `bucket-18`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx182 ON `bucket-18`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx183 ON `bucket-18`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx184 ON `bucket-18`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx185 ON `bucket-18`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx186 ON `bucket-18`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx187 ON `bucket-18`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx188 ON `bucket-18`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx189 ON `bucket-18`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx180 ON `bucket-18`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx191 ON `bucket-19`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx192 ON `bucket-19`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx193 ON `bucket-19`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx194 ON `bucket-19`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx195 ON `bucket-19`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx196 ON `bucket-19`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx197 ON `bucket-19`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx198 ON `bucket-19`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx199 ON `bucket-19`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx190 ON `bucket-19`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx201 ON `bucket-20`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx202 ON `bucket-20`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx203 ON `bucket-20`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx204 ON `bucket-20`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx205 ON `bucket-20`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx206 ON `bucket-20`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx207 ON `bucket-20`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx208 ON `bucket-20`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx209 ON `bucket-20`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx200 ON `bucket-20`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx211 ON `bucket-21`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx212 ON `bucket-21`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx213 ON `bucket-21`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx214 ON `bucket-21`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx215 ON `bucket-21`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx216 ON `bucket-21`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx217 ON `bucket-21`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx218 ON `bucket-21`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx219 ON `bucket-21`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx210 ON `bucket-21`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx221 ON `bucket-22`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx222 ON `bucket-22`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx223 ON `bucket-22`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx224 ON `bucket-22`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx225 ON `bucket-22`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx226 ON `bucket-22`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx227 ON `bucket-22`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx228 ON `bucket-22`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx229 ON `bucket-22`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx220 ON `bucket-22`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx231 ON `bucket-23`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx232 ON `bucket-23`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx233 ON `bucket-23`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx234 ON `bucket-23`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx235 ON `bucket-23`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx236 ON `bucket-23`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx237 ON `bucket-23`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx238 ON `bucket-23`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx239 ON `bucket-23`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx230 ON `bucket-23`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx241 ON `bucket-24`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx242 ON `bucket-24`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx243 ON `bucket-24`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx244 ON `bucket-24`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx245 ON `bucket-24`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx246 ON `bucket-24`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx247 ON `bucket-24`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx248 ON `bucket-24`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx249 ON `bucket-24`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx240 ON `bucket-24`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx251 ON `bucket-25`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx252 ON `bucket-25`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx253 ON `bucket-25`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx254 ON `bucket-25`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx255 ON `bucket-25`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx256 ON `bucket-25`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx257 ON `bucket-25`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx258 ON `bucket-25`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx259 ON `bucket-25`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx250 ON `bucket-25`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx261 ON `bucket-26`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx262 ON `bucket-26`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx263 ON `bucket-26`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx264 ON `bucket-26`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx265 ON `bucket-26`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx266 ON `bucket-26`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx267 ON `bucket-26`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx268 ON `bucket-26`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx269 ON `bucket-26`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx260 ON `bucket-26`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx271 ON `bucket-27`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx272 ON `bucket-27`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx273 ON `bucket-27`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx274 ON `bucket-27`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx275 ON `bucket-27`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx276 ON `bucket-27`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx277 ON `bucket-27`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx278 ON `bucket-27`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx279 ON `bucket-27`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx270 ON `bucket-27`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx281 ON `bucket-28`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx282 ON `bucket-28`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx283 ON `bucket-28`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx284 ON `bucket-28`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx285 ON `bucket-28`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx286 ON `bucket-28`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx287 ON `bucket-28`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx288 ON `bucket-28`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx289 ON `bucket-28`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx280 ON `bucket-28`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx291 ON `bucket-29`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx292 ON `bucket-29`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx293 ON `bucket-29`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx294 ON `bucket-29`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx295 ON `bucket-29`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx296 ON `bucket-29`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx297 ON `bucket-29`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx298 ON `bucket-29`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx299 ON `bucket-29`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx290 ON `bucket-29`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx301 ON `bucket-30`(alt_email, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx302 ON `bucket-30`(name, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx303 ON `bucket-30`(city, name) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx304 ON `bucket-30`(alt_email, city) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx305 ON `bucket-30`(alt_email, coins) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx306 ON `bucket-30`(coins, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx307 ON `bucket-30`(alt_email, category) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx308 ON `bucket-30`(alt_email, email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx309 ON `bucket-30`(email, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    CREATE INDEX idx300 ON `bucket-30`(city, alt_email) PARTITION BY HASH(META().id) WITH {"defer_build": true};
    BUILD INDEX ON `bucket-1`(idx11, idx12, idx13, idx14, idx15, idx16, idx17, idx18, idx19, idx10);
    BUILD INDEX ON `bucket-2`(idx21, idx22, idx23, idx24, idx25, idx26, idx27, idx28, idx29, idx20);
    BUILD INDEX ON `bucket-3`(idx31, idx32, idx33, idx34, idx35, idx36, idx37, idx38, idx39, idx30);
    BUILD INDEX ON `bucket-4`(idx41, idx42, idx43, idx44, idx45, idx46, idx47, idx48, idx49, idx40);
    BUILD INDEX ON `bucket-5`(idx51, idx52, idx53, idx54, idx55, idx56, idx57, idx58, idx59, idx50);
    BUILD INDEX ON `bucket-6`(idx61, idx62, idx63, idx64, idx65, idx66, idx67, idx68, idx69, idx60);
    BUILD INDEX ON `bucket-7`(idx71, idx72, idx73, idx74, idx75, idx76, idx77, idx78, idx79, idx70);
    BUILD INDEX ON `bucket-8`(idx81, idx82, idx83, idx84, idx85, idx86, idx87, idx88, idx89, idx80);
    BUILD INDEX ON `bucket-9`(idx91, idx92, idx93, idx94, idx95, idx96, idx97, idx98, idx99, idx90);
    BUILD INDEX ON `bucket-10`(idx101, idx102, idx103, idx104, idx105, idx106, idx107, idx108, idx109, idx100);
    BUILD INDEX ON `bucket-11`(idx111, idx112, idx113, idx114, idx115, idx116, idx117, idx118, idx119, idx110);
    BUILD INDEX ON `bucket-12`(idx121, idx122, idx123, idx124, idx125, idx126, idx127, idx128, idx129, idx120);
    BUILD INDEX ON `bucket-13`(idx131, idx132, idx133, idx134, idx135, idx136, idx137, idx138, idx139, idx130);
    BUILD INDEX ON `bucket-14`(idx141, idx142, idx143, idx144, idx145, idx146, idx147, idx148, idx149, idx140);
    BUILD INDEX ON `bucket-15`(idx151, idx152, idx153, idx154, idx155, idx156, idx157, idx158, idx159, idx150);
    BUILD INDEX ON `bucket-16`(idx161, idx162, idx163, idx164, idx165, idx166, idx167, idx168, idx169, idx160);
    BUILD INDEX ON `bucket-17`(idx171, idx172, idx173, idx174, idx175, idx176, idx177, idx178, idx179, idx170);
    BUILD INDEX ON `bucket-18`(idx181, idx182, idx183, idx184, idx185, idx186, idx187, idx188, idx189, idx180);
    BUILD INDEX ON `bucket-19`(idx191, idx192, idx193, idx194, idx195, idx196, idx197, idx198, idx199, idx190);
    BUILD INDEX ON `bucket-20`(idx201, idx202, idx203, idx204, idx205, idx206, idx207, idx208, idx209, idx200);
    BUILD INDEX ON `bucket-21`(idx211, idx212, idx213, idx214, idx215, idx216, idx217, idx218, idx219, idx210);
    BUILD INDEX ON `bucket-22`(idx221, idx222, idx223, idx224, idx225, idx226, idx227, idx228, idx229, idx220);
    BUILD INDEX ON `bucket-23`(idx231, idx232, idx233, idx234, idx235, idx236, idx237, idx238, idx239, idx230);
    BUILD INDEX ON `bucket-24`(idx241, idx242, idx243, idx244, idx245, idx246, idx247, idx248, idx249, idx240);
    BUILD INDEX ON `bucket-25`(idx251, idx252, idx253, idx254, idx255, idx256, idx257, idx258, idx259, idx250);
    BUILD INDEX ON `bucket-26`(idx261, idx262, idx263, idx264, idx265, idx266, idx267, idx268, idx269, idx260);
    BUILD INDEX ON `bucket-27`(idx271, idx272, idx273, idx274, idx275, idx276, idx277, idx278, idx279, idx270);
    BUILD INDEX ON `bucket-28`(idx281, idx282, idx283, idx284, idx285, idx286, idx287, idx288, idx289, idx280);
    BUILD INDEX ON `bucket-29`(idx291, idx292, idx293, idx294, idx295, idx296, idx297, idx298, idx299, idx290);
    BUILD INDEX ON `bucket-30`(idx301, idx302, idx303, idx304, idx305, idx306, idx307, idx308, idx309, idx300);

[access]
updates = 5
reads = 5
items = 100000
throughput = 333
spring_batch_size = 10
workers = 1
n1ql_queries = range-scan1,range-scan2,range-scan3,range-scan4,range-scan5,range-scan6,range-scan7,range-scan8,range-scan9,range-scan10,range-scan11,range-scan12,range-scan13,range-scan14,range-scan15,range-scan16,range-scan17,range-scan18,range-scan19,range-scan20,range-scan21,range-scan22,range-scan23,range-scan24,range-scan25,range-scan26,range-scan27,range-scan28,range-scan29,range-scan30
n1ql_batch_size = 2
n1ql_workers = 1
n1ql_throughput = 2

[n1ql-range-scan1]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-1` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan2]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-2` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan3]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-3` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan4]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-4` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan5]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-5` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan6]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-6` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan7]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-7` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan8]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-8` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan9]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-9` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan10]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-10` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan11]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-11` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan12]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-12` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan13]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-13` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan14]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-14` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan15]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-15` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan16]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-16` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan17]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-17` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan18]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-18` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan19]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-19` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan20]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-20` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan21]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-21` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan22]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-22` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan23]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-23` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan24]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-24` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan25]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-25` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan26]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-26` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan27]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-27` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan28]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-28` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan29]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-29` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []

[n1ql-range-scan30]
statement = SELECT name AS _name, alt_email AS _alt_email FROM `bucket-30` WHERE alt_email > 'a0' limit 1;
scan_consistency = not_bounded
args = []
